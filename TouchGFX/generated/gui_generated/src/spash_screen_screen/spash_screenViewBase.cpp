/*********************************************************************************/
/********** THIS FILE IS GENERATED BY TOUCHGFX DESIGNER, DO NOT MODIFY ***********/
/*********************************************************************************/
#include <gui_generated/spash_screen_screen/spash_screenViewBase.hpp>
#include <touchgfx/Color.hpp>
#include "BitmapDatabase.hpp"

spash_screenViewBase::spash_screenViewBase() :
    delayInteractionCounter(0)
{

    __background.setPosition(0, 0, 320, 240);
    __background.setColor(touchgfx::Color::getColorFrom24BitRGB(0, 0, 0));

    image1.setXY(0, 0);
    image1.setBitmap(touchgfx::Bitmap(BITMAP_SPLASH_ID));

    add(__background);
    add(image1);
}

void spash_screenViewBase::setupScreen()
{

}

//Handles delays
void spash_screenViewBase::handleTickEvent()
{
    if(delayInteractionCounter > 0)
    {
        delayInteractionCounter--;
        if(delayInteractionCounter == 0)
        {
            //Interaction3
            //When delayInteraction completed change screen to main_menu
            //Go to main_menu with block transition
            application().gotomain_menuScreenBlockTransition();
        }
    }
}

//Called when the screen transition ends
void spash_screenViewBase::afterTransition()
{
    //delayInteraction
    //When screen transition ends delay
    //Delay for 4000 ms (240 Ticks)
    delayInteractionCounter = DELAYINTERACTION_DURATION;
}
